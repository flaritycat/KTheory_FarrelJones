# Open Group Classes Ledger

## Status

Initial candidate list only. No class is marked open or solved without source
verification.

| Candidate class | Current project status | Known subclasses to check | Relevant modules | Next action |
|---|---|---|---|---|
| Artin groups | Selected by `FJ68` as bounded target packet `A-001`; `FJ69` creates `ledgers/artin_subclass_gap_inventory.md`; `FJ70` records no current Wu-filter candidate from existing Artin gap rows; `FJ71` pauses the active Artin proof/source lane; `FJ72` keeps Artin deferred in the target-pivot refresh; `FJ77` and `FJ81` again record that the `OBL-ARTIN-004` reactivation gate is not satisfied. Several subclasses are source-verified, but no global all-Artin theorem is recorded; `FJ02` supplies the first-pass source-label policy for `FJCw`/`FICwF`. | `ART-GAP-001`: all Artin groups; `ART-GAP-002`: unnamed subclasses outside `FJ18`; `ART-GAP-003`: possible Wu clique-reduction target not already covered by `FJ18`, now no-current-candidate and paused until a named payload appears. | FJ18, FJ20, FJ68+ | Paused as active lane after `FJ71` and deferred again by `FJ72`, `FJ77`, and `FJ81`; do not continue source work without a named payload and changed project object satisfying `OBL-ARTIN-004`. |
| Torsion-free one-relator groups | Selected first serious target `T-001`; global class remains unresolved inside project. `FJ19` records the open-status warning for Full Farrell--Jones and verified conditional routes. `FJ23`--`FJ27` record the hyperbolic, finite-dimensional CAT(0), virtually solvable, free-by-cyclic/hyperbolic-by-cyclic, and version-aware inheritance routes. `FJ28` creates `ledgers/t001_residual.md`. `FJ30`--`FJ41` record Brown/BNS/kernel-recognition data, with the concrete \(G_{2,3}\) and \(G_{p,q}\) routes already removed through `FJ26`. `FJ43` pauses automatic `RB-004` source-cluster continuation. `FJ50` pauses `RB-005`. `FJ53` is WIP / provisional and records \(\pi(w)>2\) only as hyperbolic-route overlap through `FJ23`. `FJ54` demotes `RB-006`, creates `ledgers/t001_candidate_inventory.md`, and selects the `RB-003` + `RB-004`/`RB-008` hybrid attack packet. `FJ55` records that there is no direct hierarchy-to-FJ bridge currently available and adds route-output proof obligations. `FJ56` records that all current concrete kernel-control candidate rows are calibration-only or already routed, leaving no live non-routed candidate in the inventory. `FJ57` records that the selected hybrid packet is blocked at candidate production. `FJ58` records that no replacement candidate-production lane is currently ready and selects a bounded weaker \(K_0\) / Cohen--Lyndon consequence intake. `FJ59` records that no source payload for that weaker lane is currently available. `FJ60` closes the weaker consequence lane as inactive until an exact source payload is named. `reflections/cycle_003_reflection.md` closes cycle 003 and records that `T-001` is not currently candidate-ready. `FJ61` records the candidate-intake gate. `FJ62` prunes active blockers. `FJ63` selects `DAP-T001-001`, candidate-row acquisition from current repository records. `FJ64` executes that packet and records a no-candidate note. `FJ65` records no prior-art blocker object and sends the project to the branch checkpoint. `FJ66` pauses `T-001` as an active proof-target sequence; the target remains unresolved and archived. `FJ67` records target-pivot criteria and `OBL-PIVOT-001`; no new active target is selected. `FJ68` keeps `T-001` dormant and does not satisfy the reactivation gate. `FJ72` keeps `T-001` dormant. `FJ77` and `FJ81` again record that `OBL-T001-013` is not satisfied. `FJ83` later verifies accepted weaker \(K_0\) / Cohen--Lyndon source payload `PAY-T001-K0-CL-2025-001`, but records no candidate promotion, no full `T-001` theorem, and no residual subtraction. `FJ84` audits the current rows against that source-hypothesis package and finds no FJ83-eligible current row. | Brown/BNS criterion in `ledgers/t001_kernel_recognition.md`; calibration example in `FJ31`; \(G_{2,3}\) route bridge in `FJ33`; \(G_{p,q}\)-family route subtraction in `FJ36`; BNS theorem map in `FJ41`; finite-index / `FJCw` bridge limits from `FJ45`--`FJ49`; `RB-006` only if a genuinely non-hyperbolic bridge appears; `OBL-T001-001` through `OBL-T001-015` in `ledgers/t001_candidate_inventory.md`; `OBL-PIVOT-001` in `FJ67`. | FJ19, FJ20+ | Dormant for proof-target purposes. Future use of the FJ83 source must satisfy `OBL-T001-015`; future numbered work also needs a new accepted payload. |
| Automatic/biautomatic groups | Unresolved inside project; deferred by the `FJ68` target-pivot matrix, again by the `FJ72` refresh, and again by `FJ77` and `FJ81` because no exact source payload or bounded candidate object is recorded. | CAT(0)-related, mapping class-related examples. | FJ20+, FJ68, FJ72, FJ77, FJ81 | Remain deferred unless a later target-pivot refresh records a source-ready packet or bounded candidate object. |
| Thompson-type groups | Unresolved inside project; deferred by the `FJ68` target-pivot matrix, again by the `FJ72` refresh, and again by `FJ77` and `FJ81` because no exact source payload or bounded candidate object is recorded. | F, T, V and relatives. | FJ20+, FJ68, FJ72, FJ77, FJ81 | Remain deferred unless a later target-pivot refresh records a source-ready packet or bounded candidate object. |

## FJ81 reactivation gate audit update

`FJ81` checks this ledger against the cycle-005 reactivation audit and finds
no gate-ready group-class packet. `T-001` remains dormant, Artin groups
remain paused, and automatic / biautomatic and Thompson-type groups remain
deferred.

## FJ82 payload protocol update

`FJ82` creates `ledgers/payload_intake_protocol.md` and places the project in
payload-gated pause `C5-PAUSE-001`.

No group-class target is active. A future group-class module requires an
accepted payload that changes a candidate inventory, residual-bucket status,
open-question status, proof obligation, formulation-safety bridge, or
prior-art blocker list.

## FJ83 weaker K0 payload update

`FJ83` verifies accepted payload `PAY-T001-K0-CL-2025-001` for the `T-001`
weaker \(K_0\) / Cohen--Lyndon lane only.

No group-class target becomes active from this payload. It changes source
payload status and proof obligations, but it does not prove Farrell--Jones
for torsion-free one-relator groups and does not remove a residual bucket.

## FJ84 K0 / Cohen--Lyndon candidate audit update

`FJ84` audits current `T-001` candidate rows against the FJ83 weaker \(K_0\)
/ Cohen--Lyndon source-hypothesis package.

No group-class target becomes active from this audit. No current row is
FJ83-eligible, and the audit does not prove Farrell--Jones for torsion-free
one-relator groups, does not prove coefficient FJC, `FJCw`, or `FICwF`, and
does not remove a residual bucket.
