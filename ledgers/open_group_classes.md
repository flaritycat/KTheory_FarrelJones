# Open Group Classes Ledger

## Status

Initial candidate list only. No class is marked open or solved without source verification, because apparently mathematics works better when assertions are not just decorative noises.

| Candidate class | Current project status | Known subclasses to check | Relevant modules | Next action |
|---|---|---|---|---|
| Artin groups | partially resolved inside project: several subclasses are source-verified, but no global all-Artin theorem is recorded; deferred by `FJ20`; `FJ02` now supplies the first-pass source-label policy for `FJCw`/`FICwF` | subclasses still outside the FJ18 rows; possible clique-reduction targets | FJ18, FJ20+ | Return after the current `T-001` residual pass or a deliberate Artin-target selection. |
| Torsion-free one-relator groups | selected first serious target `T-001`; global class remains unresolved inside project; `FJ19` records Lueck's open-status warning for Full Farrell--Jones and verified conditional routes; `FJ22` records the adopted one-relator hierarchy vocabulary; `FJ23` subtracts the hyperbolic route; `FJ24` subtracts the finite-dimensional CAT(0) route and compact special bridge cases; `FJ25` subtracts the virtually solvable route only when a virtual-solvability bridge is recorded; `FJ26` subtracts the hyperbolic-by-cyclic/free-by-cyclic route only when a mapping-torus or finite-rank free-by-cyclic bridge is recorded; `FJ27` subtracts version-aware inheritance routes only when exact inheritance bridges are recorded; `FJ28` creates the conservative residual ledger in `ledgers/t001_residual.md`; `FJ29` selects `RB-004` as the next attack surface; `FJ30` verifies Brown as a limited two-generator kernel-recognition source; `FJ31` runs the first Brown calibration case; `FJ32` selects \(G_{2,3}\) as the next nontrivial Brown test; `FJ33` records \(G_{2,3}\cong F_2\rtimes\mathbb Z\); `FJ34` records the \(G_{p,q}\)-family Brown-positive finite-generation boundary; `FJ35` selects a Bass--Serre freeness source route for \(K_{p,q}\); `FJ36` verifies the bridge and removes the \(G_{p,q}\)-family through `FJ26`; `FJ37` audits the residual and selects beyond-Brown `RB-004` source selection as the next target; `FJ38` selects the BNS/Bieri--Renz/Bieri/Karrass--Solitar source cluster for verification; `FJ39` verifies the Karrass--Solitar finitely presented normal-subgroup bridge; `FJ40` selects no new non-Brown source-ready finitely presented-kernel test case; `reflections/cycle_002_reflection.md` selects direct BNS theorem verification as `FJ41`; `FJ41` verifies BNS Theorem B1 but records no residual subtraction; `FJ42` checks Bieri source access and records no theorem-use upgrade or residual subtraction; `FJ43` pauses automatic `RB-004` source-cluster continuation; `FJ44` selects `RB-005` finite-index formulation handling as the next attack packet; `FJ45` records that only the full \(\mathcal{FJ}\) finite-index bridge is currently licensed; `FJ46` selects an `FJ02` source-convention interruption; `FJ02` supplies the additive-category source convention and returns `RB-005` to finite-index bridge selection | selected residual bucket: `RB-004`, finite-rank free-kernel recognition over \(\mathbb Z\); Brown/BNS criterion in `ledgers/t001_kernel_recognition.md`; first calibration example in `FJ31`; concrete \(G_{2,3}\) route bridge in `FJ33`; \(G_{p,q}\) family finite-generation boundary in `FJ34`; Bass--Serre source verification and family route subtraction in `FJ36`; beyond-Brown source-selection target selected in `FJ37`; normal-subgroup source cluster selected in `FJ38`; finitely presented-kernel subroute verified in `FJ39`; no new candidate selected in `FJ40`; direct BNS finite-generation theorem verified in `FJ41`; Bieri source-access obstruction recorded in `FJ42`; route-delta pause recorded in `FJ43`; `RB-005` attack packet selected in `FJ44`; finite-index formulation checkpoint completed in `FJ45`; source-convention interruption selected in `FJ46` and completed in `FJ02`; other residual buckets remain deferred in `ledgers/t001_residual.md` | FJ19, FJ20+ | Begin `FJ47`, coefficient finite-index bridge source selection. |
| Automatic/biautomatic groups | unresolved inside project | CAT(0)-related, mapping class-related examples | FJ20+ | Defer until known-cases ledger. |
| Thompson-type groups | unresolved inside project | F, T, V and relatives | FJ20+ | Defer until known-cases ledger. |
